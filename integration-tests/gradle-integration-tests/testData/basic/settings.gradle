pluginManagement {
    repositories {
        maven { url 'https://dl.bintray.com/kotlin/kotlin-dev/' }
        mavenLocal()
        mavenCentral()
        jcenter()
        gradlePluginPortal()
    }
}

rootProject.name = "basic"
