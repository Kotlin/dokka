/**
 * Support for rendering passed attributes via `DocumentationOptions`.
 *
 * Custom attributes::
 * `+{project_name}+`;; {project_name}
 * `+{project_desc}+`;; {project_desc}
 * `+{project_version}+`;; {project_version}
 *
 * NOTE: The attributes must be passed via the `DocumentationOptions` _additionalParams_.
 *
 */
fun asciidocAttributes() {
}