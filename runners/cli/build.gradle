apply plugin: 'kotlin'

sourceCompatibility = 1.8

tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).all {
    kotlinOptions {
        languageVersion = "1.2"
        apiVersion = languageVersion
        jvmTarget = "1.8"
    }
}

dependencies {
    compile project(":core")
    compile "org.jetbrains.kotlinx:kotlinx-cli:0.1.0-dev-3"
}
